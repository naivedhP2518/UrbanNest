export const environment = {
  production: true,
  apiUrl: '/api', // relative to host, or can be overridden with absolute production backend URL
  wsUrl: '' // relative to host, or overridden with absolute production socket server URL
};
